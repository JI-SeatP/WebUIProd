### CREATE
You are an expert in software reverse engineering, software architecture analysis and software replication.

You need to create in the docs/status_action_button/ folder an extensive audit documentation that break downs and explain exactly each step that happens on how the status buttons , status action button in the operation screen.
Evert single input (source, format, sequence of execution), and ouputs (formats, sequence of execution, stored procedures and database tables used used) needs to be understood, mapped in the correct order so that a developer can replicate the exact same logic on other tech stacks. You can't assume anything that is not backed by the code. If something is not clear, trigger another agent to get you more 

Subdivide the work into logical sections if needed and assign subagents to each section. You should review that their output is correct and that it matches the rest of the code. If one subagent surfaces something that requires revision of another related section, pass this revision to its related subagent. Your main task is to get full consistent crystal clear documentation of the exact logic of this functionality out of this team.

### REVIEW
You are an expert in software reverse engineering, software architecture analysis and software replication. You need to update/revise the documentation in docs/status_action_button/ folder an extensive audit documentation that break downs and explain exactly each step that happens on how the status buttons , status action button in the operation screen. Evert single input (source, format, sequence of execution), and ouputs (formats, sequence of execution, stored procedures and database tables used used) needs to be understood, mapped in the correct order so that a developer can replicate the exact same logic on other tech stacks. You can't assume anything that is not backed by the code. If something is not clear, trigger another agent to get you more Subdivide the work into logical sections if needed and assign subagents to each section. You should review that their output is correct and that it matches the rest of the code. If one subagent surfaces something that requires revision of another related section, pass this revision to its related subagent. Your main task is to get full consistent crystal clear documentation of the exact logic of this functionality out of this team.

### CHANGE NEW CODE

You are an expert in software development, software reverse engineering, software architecture analysis and software replication. You need to update the existing behaviour of the status action button to perform the exact same logic and update the exact same tables in database using same store procedures and passing same arguments in same order and same format. The goal is to ensure that the new tech stack can replicate the exact same results on database as the old software. The design, look and feel, layout and positions of the new version should be kept. It is the logic of execution, database and logic of button  availability what needs to be improved. To do this, you need to use as main source of truth the extensive research from C:\DEVPROJECTS\WebUIProd\docs\status_action_button folder. Start with 07-old-vs-new-comparison and use the other sections for additional context. 
Deploy a team of agents to work on the different sections and control all their work to make sure it is coherent and consistent.  You can't assume anything that is not backed by the code. If something is not clear, trigger another agent to get you more Subdivide the work into logical sections if needed and assign subagents to each section. You should review that their output is correct and that it matches the rest of the code. If one subagent surfaces something that requires revision of another related section, pass this revision to its related subagent. 

### Status read
You are an expert in software reverse engineering, software architecture analysis and software replication. You need to document exactly how the old software button gets the status when the operation screen loads. Make sure that the exact same status is passed in the exact same way. If upstream formulas/variables are required, you need to understand them too. 

Compare this to the current documentation in docs/status_action_button/ folder and correct any discrepancies on the docs that do not follow your findings.

Every single input (source, format, sequence of execution), and ouputs (formats, sequence of execution, stored procedures and database tables used used) needs to be understood, mapped in the correct order so that a developer can replicate the exact same logic on other tech stacks. 

You can't assume anything that is not backed by the code. If something is not clear, trigger another agent to get you more Subdivide the work into logical sections if needed and assign subagents to each section. You should review that their output is correct and that it matches the rest of the code. If one subagent surfaces something that requires revision of another related section, pass this revision to its related subagent. Your main task is to get full consistent crystal clear documentation of the exact logic of this functionality out of this team.